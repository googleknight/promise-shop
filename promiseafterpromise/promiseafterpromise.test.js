const chaining = require('./promiseafterpromise');

describe('Chekcing the returned value ', () => {
  it('Checking the returned value', (done) => {
    // function callback(data) {
    //   expect(typeof data).toBe();
    //   done();
    // }
    // chaining(callback);
  });
});
